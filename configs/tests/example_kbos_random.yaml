defaults:
  - default.yaml


airport: 'kbos'

scene_files: KBOS_26_1672621200,KBOS_23_1672610400
ckpt_path: 'datasets/amelia/checkpoints/Single-Airport/kbos_/kbos_random.ckpt'

# Model/Data settings
sampling_type: random

use_map : True

# Testing options
plot: True
# plotting all agents
plot_all: True

from_pickle: False
report_inference_time: False

# scene_type from scene_types
scene_type: marginal_pred
max_scenes: 20



scene_option:
  # scene_type: marginal_pred
  marginal_pred:
  # scene_type: benchmark
  benchmark:
    agents: []

  collision: